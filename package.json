{
  "name": "@actionsx/prettier",
  "version": "0.0.0",
  "scripts": {
    "build": "ncc build index.js",
    "check": "prettier --check .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "prettier": "^2.8.0",
    "shell-quote": "^1.7.3"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.34.0"
  }
}

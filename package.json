{
  "name": "@actionsx/prettier",
  "version": "0.0.0",
  "scripts": {
    "build": "ncc build index.js",
    "check": "prettier --check .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@actions/core": "^1.5.0",
    "prettier": "^2.3.2",
    "shell-quote": "^1.7.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.30.0"
  }
}
